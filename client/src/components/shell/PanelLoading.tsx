/** Task 071 (MX-006) — what a lazy surface shows while its chunk arrives. */
export function PanelLoading() {
  return (
    <p role="status" className="px-6 py-8 text-center text-[13px] text-text-dim">
      Loading…
    </p>
  );
}
